dsn: postgres://admin:pvFtFhvD-LtKbw-1PCwNg0Tfjd-PPc@postgresd:5432/charming-wolf?sslmode=disable

serve:
  public:
    base_url: http://kratos:4433/
    cors:
      enabled: true
  admin:
    base_url: https://kratos-new.onrender.com/admin

log:
  level: debug
  format: text
  leak_sensitive_values: true

secrets:
  cookie:
    - PLEASE-CHANGE-ME-I-AM-VERY-INSECURE
  cipher:
    - 32-LONG-SECRET-NOT-SECURE-AT-ALL
    # Secrets must be 32-character-long strings.

ciphers:
  algorithm: xchacha20-poly1305

hashers:
  algorithm: bcrypt
  bcrypt:
    cost: 12
    # BCrypt has a maximum length of 72 bytes for passwords. If a longer password is attempted to be used, an error will be returned to the user

identity:
  default_schema_id: default
  schemas:
    - id: default
      url: file:///etc/config/kratos/identity.schema.json

feature_flags:
  use_continue_with_transitions: true
